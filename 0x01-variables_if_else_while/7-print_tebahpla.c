#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - prints lower case alpabets in reverse
 * followed by a new line.
 * Return: Always 0 success
 */

int main(void);
{
	char ch;

	for (cha = 'z'; ch >= 'a'; ch--)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
